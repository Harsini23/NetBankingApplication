<Page
    x:Class="NetBankingApplication.View.UserControls.FullAccountDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetBankingApplication.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!--<TextBlock VerticalAlignment="Center" Text="{x:Bind expense,Mode=OneWay}"></TextBlock>
        <TextBlock Text="{x:Bind CurrentSelectedAccount.AccountNumber}"></TextBlock>-->

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <!--₹-->
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock FontFamily="Arial" FontSize="20" Text="Account No:"></TextBlock>
                <TextBlock FontFamily="Arial" FontSize="20" Text="{x:Bind CurrentSelectedAccount.AccountNumber,Mode=OneWay}"></TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                <TextBlock FontWeight="SemiLight" FontSize="12" Text="Your last transaction was on"></TextBlock>
                <TextBlock FontWeight="SemiLight" FontSize="12" Text="{x:Bind AccountTransactionsViewModel.CurrentMonthExpense,Mode=OneWay}"></TextBlock>
                <TextBlock FontWeight="SemiLight" FontSize="12" Text="{x:Bind expense,Mode=OneWay}"></TextBlock>
            </StackPanel>
            <TextBlock FontWeight="SemiLight" FontSize="12" Grid.Row="0" Grid.Column="1" Text="Total Balance"></TextBlock>
            <StackPanel  Grid.Row="1" Grid.Column="1">
                <TextBlock FontFamily="Arial" FontSize="20" Text="₹"></TextBlock>
                <TextBlock FontFamily="Arial" FontSize="20" Text="{x:Bind CurrentSelectedAccount.TotalBalance,Mode=OneWay}"></TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
