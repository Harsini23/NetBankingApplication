<UserControl
    x:Class="NetBankingApplication.View.UserControls.TransactionHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetBankingApplication.View.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data ="using:Library.Model"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    Loaded="UserControl_Loaded"
    d:DesignWidth="400">
    <!--<UserControl.Resources>

        --><!--<DataTemplate x:Name="TransactionListViewTemplate" x:DataType="data:Transaction">
        </DataTemplate>--><!--

    </UserControl.Resources>-->

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ListView ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollMode="Enabled" ItemsSource="{x:Bind TransactionViewModel.AllTransactions,Mode=OneWay}" IsItemClickEnabled="True"  SelectionMode="Single"  x:Name="AllTransactionListView" >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Transaction">
                    <Grid Margin="10" BorderBrush="Black" BorderThickness="0.3" CornerRadius="30" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                            <ColumnDefinition Width="100"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"></RowDefinition>
                            <RowDefinition Height="1*"></RowDefinition>
                            <RowDefinition Height="1*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <PersonPicture Margin="5" Initials="H" Grid.Column="0" Width="50" Height="50" Grid.RowSpan="3"/>
                        <TextBlock Margin="5" Text="{x:Bind ToAccount}" Grid.Column="1" Grid.Row="0"></TextBlock>
                        <TextBlock Margin="5" Text="{x:Bind Remark}" Grid.Column="1" Grid.Row="1"></TextBlock>
                        <TextBlock Margin="5" Text="{x:Bind Date}" Grid.Column="1" Grid.Row="2"></TextBlock>
                        <TextBlock Margin="5" Text="{x:Bind TransactionAmout}" Grid.Column="2" Grid.RowSpan="3" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
