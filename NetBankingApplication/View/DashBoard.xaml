<Page
    x:Class="NetBankingApplication.View.DashBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetBankingApplication.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger
                        MinWindowWidth="{x:Bind DashBoardNavigation.CompactModeThresholdWidth}" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="DashBoardNavigation.PaneDisplayMode" Value="Top"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <NavigationView x:Name="DashBoardNavigation" Header="Welcome user" SelectionChanged="DashBoardNavigation_SelectionChanged" IsBackButtonVisible="Collapsed" IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem  Content="Overview" x:Name="Overview" />
                <NavigationViewItem  Content="Bank Account" x:Name="BankAccount"  />
                <NavigationViewItem  Content="Payments and Transfer" x:Name="PaymentsAndTransfer"  />
                <NavigationViewItem  Content="Card and Loans" x:Name="CardAndLoans"  />
                <NavigationViewItem  Content="Investment and Insurance" x:Name="InvestmentAndInsurance"  />
                <NavigationViewItem  Content="Customer Service" Name="CustomerService" />
            </NavigationView.MenuItems>
            <Frame x:Name="SelectedDashboardContentFrame"/>
        </NavigationView>


    </Grid>
</Page>
